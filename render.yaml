services:
  - type: web
    name: demand-signal-api
    runtime: docker
    region: singapore
    dockerfilePath: ./backend/Dockerfile
    healthCheckPath: /health
    envVars:
      - key: ENABLE_API_DOCS
        value: "false"
